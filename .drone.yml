pipeline:
  install:
    image: node:latest
    commands:
      - npm install
  build:
    image: node:latest
    commands:
      - npm run build
  lint:
    image: node:latest
    commands: npm run lint
